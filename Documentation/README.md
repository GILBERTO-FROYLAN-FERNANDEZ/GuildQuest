To run, simply run the `Engine`'s `main()` method.
You will be prompted for input via the **terminal**, usually a _number_ for
choosing among various choices. You may be prompted to
type some _string_(s) (ie: to specify a name).


